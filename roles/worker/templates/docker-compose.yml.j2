# Generated by ansible

version: "2"
services:
  worker:
    image: {{ concourse_version }}
    privileged: true
    command: worker
    volumes: ["./keys:/concourse-keys"]
    environment:
      CONCOURSE_TSA_HOST: {{ tsa_default_interface_ip }}
      CONCOURSE_TSA_PUBLIC_KEY: /concourse-keys/tsa_host_key.pub
