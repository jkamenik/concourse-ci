# Generated by ansible

version: "2"
services:
  worker:
    image: {{ concourse_version }}
    privileged: true
    command: worker
    volumes: ["./keys:/concourse-keys"]
    environment:
      CONCOURSE_TSA_HOST: {{ hostvars[tsa_hostname]['ansible_default_ipv4']['address'] }}
      CONCOURSE_TSA_PUBLIC_KEY: /concourse-keys/tsa_host_key.pub
